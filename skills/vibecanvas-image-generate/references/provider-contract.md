# Provider contract

VibeCanvas uses one active shared provider profile. It supports custom model aliases, paths, request headers, protected URL download headers, extra JSON, declared capabilities, costs, timeout, and retries.

Generation accepts Base64 or URL image responses. URL downloads are validated for protocol, redirects, credentials, host, private addresses, and size. Output is decoded and normalized before Artifact registration.
